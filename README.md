# Basic template with Laravel & VueJS & TailwindCSS

## How to run

- `composer install`
- `npm install`
- `cp .env.example .env`
- remove `gitignore` and use `.gitignore`
- change your values
- `sail up`
- `npm run dev`
- access `localhost`